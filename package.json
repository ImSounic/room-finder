{
  "name": "room-finder",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc -b packages/core packages/adapters packages/notifier packages/scraper",
    "scrape": "node --env-file=.env packages/scraper/dist/run.js",
    "scrape:local": "npm run build && node --env-file=.env packages/scraper/dist/run.js --venue local",
    "scrape:cloud": "node packages/scraper/dist/run.js --venue cloud",
    "build": "tsc -b packages/core packages/adapters packages/notifier packages/scraper",
    "smoke": "npm run build && node packages/scraper/dist/smoke.js",
    "fb:login": "node scripts/fb-login.mjs"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vitest": "^2.1.0",
    "@types/node": "^22.0.0"
  }
}
